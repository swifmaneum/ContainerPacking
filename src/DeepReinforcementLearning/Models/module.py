class Module:
    def __init__(self, number, length, width, max_capacity):
        self.number = number
        self.length = length
        self.width = width
        self.max_capacity = max_capacity
        self.capacity = 0
